@AGENTS.md

## Claude Code

- Confirm this file and `AGENTS.md` are loaded with `/context` when beginning a new session.
- Use project subagents in `.claude/agents/` only for their stated bounded roles.
- Fiscal verification begins with an independent evidence reconstruction before reading the owner's conclusions or run record.
- Reviewer agents are read-only by default: report findings and let the task owner resolve them.
- Do not enable new MCP servers, hooks, permissions, or external capabilities without the human gate defined in `AGENTS.md`.
